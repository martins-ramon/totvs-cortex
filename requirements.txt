# Core web frameworks
flask
flask-cors
flask-sqlalchemy

# AI / HTTP
requests

# Agentic layer (FastAPI ASGI gateway)
fastapi
uvicorn
starlette

# Optional enhancements for future AI workflows and schema validation
pydantic
python-dotenv

# Optional realtime (if needed later)
flask-socketio

# ORM and migrations
sqlalchemy

pyyaml