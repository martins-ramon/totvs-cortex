Aqui está um prompt refinado e detalhado, projetado para ser enviado a uma ferramenta como o Replit, com base no seu objetivo e no código-fonte existente.

Este prompt detalha as alterações necessárias no banco de dados, backend (rotas e serviços) e frontend (HTML e JS) para implementar a primeira experiência agêntica de forma coesa com a arquitetura atual.

-----

### Prompt para Replit: Implementando o Agente "Momentum" no Cortex

**Objetivo:** Implementar uma experiência agêntica no sistema Cortex. O primeiro agente será um "Mentor de Carreira" proativo que analisa dados de feedbacks e reuniões para fornecer insights e sugestões de ação ao usuário.

**Nome do Agente:** Momentum
**Personalidade:** Um mentor de carreira solidário, analítico e focado em alta performance. Ele é proativo, identifica padrões e sugere ações concretas para o desenvolvimento do usuário.

A experiência deve ser distinta do chatbot RAG existente (que usa `/api/chat` para consulta de dados). O "Momentum" terá conversas estado-ful e tomará ações (como enviar e-mails) com a aprovação do usuário (human-in-the-loop).

Abaixo estão as modificações necessárias na base de código fornecida (Python/Flask, JS e HTML).

-----

### 1\. Alterações no Banco de Dados (`database.py`)

Precisamos de tabelas para definir os agentes e armazenar suas conversas, que são separadas das `notifications` (que são "FYI") e do chat RAG (que é sem estado).

Adicione as seguintes tabelas no final da função `init_db()`:

```python
# ... (outras tabelas) ...

        conn.execute(text("""
        CREATE TABLE IF NOT EXISTS agents (
            id SERIAL PRIMARY KEY,
            name VARCHAR(100) NOT NULL,
            personality_prompt TEXT NOT NULL,
            type VARCHAR(50) UNIQUE NOT NULL, -- ex: 'career_mentor'
            is_active BOOLEAN DEFAULT TRUE,
            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
        )"""))
        conn.commit()

        conn.execute(text("""
        CREATE TABLE IF NOT EXISTS agent_conversations (
            id SERIAL PRIMARY KEY,
            user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
            agent_id INTEGER NOT NULL REFERENCES agents(id) ON DELETE CASCADE,
            message_text TEXT NOT NULL,
            is_from_agent BOOLEAN DEFAULT FALSE,
            is_proactive_insight BOOLEAN DEFAULT FALSE, -- Marca se foi o agente que iniciou
            is_read BOOLEAN DEFAULT FALSE,
            related_action_data JSONB, -- Para ações 'human-in-the-loop' (ex: { "action": "email_manager", "subject": "..." })
            created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
        )"""))
        conn.commit()

        # Popular nosso primeiro agente (opcional, mas bom para setup)
        conn.execute(text("""
            INSERT INTO agents (name, personality_prompt, type)
            VALUES (
                'Momentum',
                'Você é Momentum, um mentor de carreira focado em alta performance. Você analisa feedbacks e reuniões passadas para encontrar padrões e oportunidades de crescimento. Você é proativo, encorajador e suas sugestões são sempre acionáveis. Responda em português brasileiro.',
                'career_mentor'
            )
            ON CONFLICT (type) DO NOTHING
        """))
        conn.commit()
```

-----

### 2\. Alterações no Backend (Novos Serviços e Rotas)

#### 2.1. Novo Arquivo: `agent_runner.py`

Precisamos de um processo em segundo plano para que os agentes sejam *proativos*. Um `APScheduler` integrado ao Flask é ideal.

Crie um novo arquivo `agent_runner.py`:

```python
from apscheduler.schedulers.background import BackgroundScheduler
from sqlalchemy import text
from database import SessionLocal
import services  # Importar os serviços de IA
import json

def get_user_context_for_agent(db, user_id):
    """Busca dados de feedbacks e reuniões para o agente."""
    
    # 1. Buscar Feedbacks
    feedbacks_res = db.execute(
        text("SELECT description, feedback_date FROM feedbacks WHERE employee_id = :uid ORDER BY feedback_date DESC LIMIT 5"),
        {"uid": user_id}
    ).fetchall()
    feedbacks = [f"Data: {f[1]}, Feedback: {f[0]}" for f in feedbacks_res]

    # 2. Buscar Reuniões
    meetings_res = db.execute(
        text("SELECT summary, meeting_date FROM meetings WHERE user_id = :uid ORDER BY meeting_date DESC LIMIT 5"),
        {"uid": user_id}
    ).fetchall()
    meetings = [f"Data: {m[1]}, Resumo: {m[0]}" for m in meetings_res]

    return f"Contexto de Feedbacks Recebidos:\n{'\n'.join(feedbacks)}\n\nContexto de Reuniões:\n{'\n'.join(meetings)}"

def run_career_mentor_agent(user_id, agent):
    """Lógica proativa do agente Momentum."""
    db = SessionLocal()
    try:
        context = get_user_context_for_agent(db, user_id)
        if not context.strip():
            print(f"Sem contexto para o usuário {user_id}, pulando.")
            return

        # 1. Verificar se o último insight ainda não foi lido
        last_insight = db.execute(
            text("""
                SELECT id FROM agent_conversations 
                WHERE user_id = :uid AND agent_id = :aid AND is_proactive_insight = TRUE AND is_read = FALSE
            """),
            {"uid": user_id, "aid": agent['id']}
        ).fetchone()
        
        if last_insight:
            print(f"Usuário {user_id} já possui um insight pendente.")
            return

        # 2. Gerar novo insight
        insight = services.generate_agent_insight(agent['prompt'], context)
        
        if insight and insight.get("proactive_message"):
            # 3. Salvar o insight proativo no banco
            db.execute(
                text("""
                    INSERT INTO agent_conversations 
                    (user_id, agent_id, message_text, is_from_agent, is_proactive_insight, is_read, related_action_data)
                    VALUES (:uid, :aid, :msg, TRUE, TRUE, FALSE, :action_data)
                """),
                {
                    "uid": user_id,
                    "aid": agent['id'],
                    "msg": insight["proactive_message"],
                    "action_data": json.dumps(insight.get("action_data")) if insight.get("action_data") else None
                }
            )
            db.commit()
            print(f"Novo insight gerado para o usuário {user_id}")

    except Exception as e:
        db.rollback()
        print(f"Erro ao rodar agente para usuário {user_id}: {e}")
    finally:
        db.close()

def agent_job():
    """Trabalho que roda periodicamente para todos os usuários."""
    print("Iniciando verificação proativa dos agentes...")
    db = SessionLocal()
    try:
        # Buscar todos os usuários e o agente mentor
        users = db.execute(text("SELECT id FROM users")).fetchall()
        mentor_agent_res = db.execute(text("SELECT id, personality_prompt FROM agents WHERE type = 'career_mentor'")).fetchone()
        
        if not mentor_agent_res:
            print("Agente 'career_mentor' não encontrado.")
            return
            
        mentor_agent = {"id": mentor_agent_res[0], "prompt": mentor_agent_res[1]}

        for user in users:
            run_career_mentor_agent(user[0], mentor_agent)

    finally:
        db.close()

def init_scheduler(app):
    """Inicializa o scheduler."""
    scheduler = BackgroundScheduler(daemon=True)
    # Roda a cada 4 horas (exemplo)
    scheduler.add_job(agent_job, 'interval', hours=4)
    scheduler.start()
```

#### 2.2. Atualização em `app.py`

Precisamos inicializar o `APScheduler`.

```python
import os
from flask import Flask, send_from_directory
from flask_cors import CORS
from database import init_db
from routes import api_bp
from agent_runner import init_scheduler # ✅ NOVO

def create_app():
    """Cria e configura uma instância da aplicação Flask."""
    app = Flask(__name__, static_folder='static')
    app.secret_key = os.environ.get('SESSION_SECRET', 'dev-secret-key-change-in-production')

    CORS(app, supports_credentials=True)
    app.register_blueprint(api_bp)

    @app.route('/')
    def index():
        # ... (código existente)
    
    # ... (código existente)

    init_scheduler(app) # ✅ NOVO: Inicia o scheduler
    return app

# ... (código existente)
```

#### 2.3. Atualização em `services.py`

Precisamos de uma nova função de IA para o agente proativo.

```python
# ... (outros imports)
import services # (Se services.py for importado em routes.py, isso pode precisar de ajuste)

# ... (outras funções)

def generate_agent_insight(agent_prompt: str, context: str):
    """
    Gera um insight proativo para um agente.
    """
    prompt = f"""{agent_prompt}

Abaixo está o contexto do usuário (feedbacks recentes e resumos de reunião):
---
{context}
---

Baseado *estritamente* neste contexto, identifique UMA oportunidade de crescimento ou UM problema recorrente.
Gere uma mensagem proativa e acionável para o usuário.
Se sua sugestão envolver outra pessoa (ex: o gestor), proponha uma ação que o usuário possa aprovar (human-in-the-loop).

Responda em JSON no formato:
{{
    "proactive_message": "A mensagem que o usuário verá. Ex: 'Notei que o tema X apareceu em 3 reuniões. Minha sugestão é...'",
    "action_data": {{ "action": "tipo_da_acao", "details": "..." }} ou null
}}

Exemplo de action_data se sugerir falar com gestor:
{{ "action": "propose_to_manager", "proposal_text": "Texto da proposta para o gestor..." }}
"""
    try:
        response = openai_client.chat.completions.create(
            model="gpt-4o", # Usar um modelo mais forte para insights
            messages=[{"role": "user", "content": prompt}],
            response_format={"type": "json_object"}
        )
        return json.loads(response.choices[0].message.content)
    except Exception as e:
        print(f"Erro ao gerar insight do agente: {e}")
        return None

def generate_agent_conversation_response(agent_prompt: str, conversation_history: list, user_question: str):
    """
    Gera uma resposta de chat para um agente (conversa reativa).
    """
    messages = [{"role": "system", "content": agent_prompt}]
    
    # Adicionar histórico (simplificado)
    for msg in conversation_history[-5:]:
        messages.append({"role": "assistant" if msg['is_from_agent'] else "user", "content": msg['message_text']})
    
    messages.append({"role": "user", "content": user_question})
    
    response = openai_client.chat.completions.create(
        model="gpt-4o-mini",
        messages=messages,
        temperature=0.7, max_tokens=500
    )
    return response.choices[0].message.content
```

#### 2.4. Atualização em `routes.py`

Adicione as novas rotas para os agentes.

```python
# ... (outros imports)

# ... (outras rotas)

# --- ROTAS DE AGENTES ---

@api_bp.route('/agents', methods=['GET'])
def get_agents():
    """Lista os agentes disponíveis e a contagem de mensagens não lidas."""
    if 'user_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    db = SessionLocal()
    try:
        agents = db.execute(text("SELECT id, name, type FROM agents WHERE is_active = TRUE")).fetchall()
        
        agent_list = []
        for agent in agents:
            unread_count_res = db.execute(
                text("SELECT COUNT(id) FROM agent_conversations WHERE user_id = :uid AND agent_id = :aid AND is_read = FALSE"),
                {"uid": session['user_id'], "aid": agent[0]}
            ).fetchone()
            agent_list.append({
                "id": agent[0],
                "name": agent[1],
                "type": agent[2],
                "unread_count": unread_count_res[0] if unread_count_res else 0
            })
        return jsonify({"agents": agent_list})
    finally:
        db.close()

@api_bp.route('/agents/<int:agent_id>/conversation', methods=['GET'])
def get_agent_conversation(agent_id):
    """Busca o histórico de conversa com um agente."""
    if 'user_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    db = SessionLocal()
    try:
        conv_res = db.execute(
            text("""
                SELECT id, message_text, is_from_agent, is_proactive_insight, is_read, related_action_data, created_at 
                FROM agent_conversations
                WHERE user_id = :uid AND agent_id = :aid
                ORDER BY created_at ASC
            """),
            {"uid": session['user_id'], "aid": agent_id}
        ).fetchall()
        
        conversation = [
            {"id": r[0], "text": r[1], "is_from_agent": r[2], "is_proactive": r[3], "is_read": r[4], "action_data": r[5], "created_at": r[6].isoformat()}
            for r in conv_res
        ]
        return jsonify({"conversation": conversation})
    finally:
        db.close()

@api_bp.route('/agents/<int:agent_id>/conversation/mark_read', methods=['POST'])
def mark_agent_conversation_as_read(agent_id):
    """Marca todas as mensagens de um agente como lidas."""
    if 'user_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    db = SessionLocal()
    try:
        db.execute(
            text("UPDATE agent_conversations SET is_read = TRUE WHERE user_id = :uid AND agent_id = :aid"),
            {"uid": session['user_id'], "aid": agent_id}
        )
        db.commit()
        return jsonify({"success": True})
    finally:
        db.close()

@api_bp.route('/agents/<int:agent_id>/message', methods=['POST'])
def post_agent_message(agent_id):
    """Envia uma mensagem para um agente e obtém uma resposta."""
    if 'user_id' not in session: return jsonify({"error": "Unauthorized"}), 401
    data = request.json
    user_question = data.get('question')
    if not user_question:
        return jsonify({"error": "Question is required"}), 400

    db = SessionLocal()
    try:
        # 1. Salvar a mensagem do usuário
        db.execute(
            text("""
                INSERT INTO agent_conversations (user_id, agent_id, message_text, is_from_agent, is_read)
                VALUES (:uid, :aid, :msg, FALSE, TRUE)
            """),
            {"uid": session['user_id'], "aid": agent_id, "msg": user_question}
        )
        db.commit()

        # 2. Obter histórico e prompt do agente
        agent = db.execute(text("SELECT id, personality_prompt FROM agents WHERE id = :aid"), {"aid": agent_id}).fetchone()
        if not agent:
            return jsonify({"error": "Agent not found"}), 404
        
        history_res = db.execute(
            text("SELECT message_text, is_from_agent FROM agent_conversations WHERE user_id = :uid AND agent_id = :aid ORDER BY created_at DESC LIMIT 5"),
            {"uid": session['user_id'], "aid": agent_id}
        ).fetchall()
        history = [{"message_text": r[0], "is_from_agent": r[1]} for r in history_res]
        history.reverse() # A ordem deve ser da mais antiga para a mais nova

        # 3. Gerar resposta
        agent_response_text = services.generate_agent_conversation_response(agent[1], history, user_question)

        # 4. Salvar resposta do agente
        db.execute(
            text("""
                INSERT INTO agent_conversations (user_id, agent_id, message_text, is_from_agent, is_read)
                VALUES (:uid, :aid, :msg, TRUE, FALSE)
            """),
            {"uid": session['user_id'], "aid": agent_id, "msg": agent_response_text}
        )
        db.commit()
        
        return jsonify({"success": True, "response": agent_response_text})
    except Exception as e:
        db.rollback()
        return jsonify({"error": str(e)}), 500
    finally:
        db.close()
```

-----

### 3\. Alterações no Frontend (UI/UX)

A IU deve refletir a distinção entre o Chat RAG (existente) e os Agentes Proativos (novos). Modificaremos o `#chat-widget` para se tornar um "Agent Dock" que lista todos os "bots" (RAG + Agentes).

#### 3.1. Alterações em `index.html`

O `#chat-widget` não será mais apenas um `div` vazio, mas sim o *container* para o "dock" e a "janela".

```html
<div class="toast-container" id="toast-container"></div>

    <div id="agent-dock-container" class="agent-dock-container">
        <button id="rag-chat-button" class="agent-dock-button" onclick="toggleChat('rag')">
            <svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 0 24 24" width="24px" fill="#FFFFFF"><path d="M0 0h24v24H0V0z" fill="none"/><path d="M21 6h-2v9H6v2c0 .55.45 1 1 1h11l4 4V7c0-.55-.45-1-1-1zm-4 6V4c0-.55-.45-1-1-1H3c-.55 0-1 .45-1 1v14l4-4h10c.55 0 1-.45 1-1z"/></svg>
            <div id="rag-chat-badge" class="agent-badge" style="display: none;"></div>
        </button>
        
        </div>

    <div id="chat-window" class="chat-window" style="display: none;">
        <div class="chat-header">
            <h3 id="chat-window-title">Assistente Cortex</h3>
            <button class="chat-close" onclick="closeChatWindow()">×</button>
        </div>
        <div id="chat-messages" class="chat-messages">
            </div>
        <div class="chat-input-container">
            <input type="text" id="chat-input" placeholder="Digite sua mensagem...">
            <button id="chat-send-btn" class="chat-send-btn">Enviar</button>
        </div>
    </div>
    <script src="/static/js/app.js"></script>
</body>
</html>
```

#### 3.2. Alterações em `components.css`

Precisamos estilizar o novo "Agent Dock" e os avatares dos agentes.

```css
/* ... (no final do arquivo) ... */

/* --- Componentes do Agent Dock --- */
.agent-dock-container {
    position: fixed;
    bottom: 2rem;
    right: 2rem;
    z-index: 9998; /* Abaixo da janela de chat */
    display: flex;
    flex-direction: column-reverse; /* Empilha botões para cima */
    gap: 1rem;
}

.agent-dock-button {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background: linear-gradient(135deg, #6366F1, #4F46E5);
    border: none;
    color: white;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 8px 24px rgba(99, 102, 241, 0.4);
    transition: all 0.3s ease;
    position: relative; /* Para o badge */
}

.agent-dock-button:hover {
    transform: scale(1.1);
    box-shadow: 0 12px 32px rgba(99, 102, 241, 0.5);
}

/* Avatar do Agente (ex: Momentum) */
.agent-avatar-button {
    background: #1E293B; /* Cor diferente para agentes */
    border: 2px solid #6366F1;
    font-size: 1.5rem;
    font-weight: 600;
}

.agent-badge {
    position: absolute;
    top: -5px;
    right: -5px;
    background-color: #EF4444;
    color: white;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.8rem;
    font-weight: 700;
    border: 2px solid white;
}

/* Janela de Chat (ajustes) */
.chat-window {
    z-index: 9999; /* Acima do dock */
    /* (Estilos existentes .chat-window, .chat-header, etc. são reutilizados) */
}

/* Estilo para mensagens proativas */
.proactive-insight {
    background-color: #F0F5FF;
    border-left: 4px solid #6366F1;
    padding: 1rem;
    border-radius: 8px;
    margin: 0.5rem 0;
}
.proactive-insight strong {
    color: #4338CA;
    display: block;
    margin-bottom: 0.5rem;
}

/* Botão de Ação (Human-in-the-loop) */
.action-button {
    background: #6366F1;
    color: white;
    border: none;
    border-radius: 6px;
    padding: 0.5rem 1rem;
    cursor: pointer;
    margin-top: 1rem;
    font-weight: 600;
}
.action-button:hover {
    background: #4F46E5;
}
```

#### 3.3. Alterações em `app.js`

Esta é a mudança mais significativa. Estamos refatorando o `chat-widget` para um `agent-dock`.

```javascript
// ... (variáveis globais no topo) ...
let currentAgentContext = null; // 'rag' ou 'agent_1', 'agent_2', etc.
let activeAgents = []; // Armazena dados dos agentes (ex: [{id: 1, name: 'Momentum'}])

// --- AUTENTICAÇÃO E INICIALIZAÇÃO ---

// Em showApp(), substitua renderChatWidget() por:
// renderChatWidget(); // 🛑 REMOVER
renderAgentDock(); // ✅ ADICIONAR
// ...
// (Remova a lógica de display: 'block' para #chat-widget)


// --- CHAT WIDGET / AGENT DOCK (SEÇÃO REESCRITA) ---

async function renderAgentDock() {
    const container = document.getElementById('agent-dock-container');
    if (!container) return;
    
    // 1. Limpa agentes antigos (exceto o RAG bot)
    container.querySelectorAll('.agent-avatar-button').forEach(btn => btn.remove());

    // 2. Busca agentes da API
    try {
        const response = await fetch('/api/agents', { credentials: 'include' });
        const data = await response.json();
        activeAgents = data.agents || [];

        activeAgents.forEach(agent => {
            const agentButton = document.createElement('button');
            agentButton.id = `agent-button-${agent.id}`;
            agentButton.className = 'agent-dock-button agent-avatar-button';
            agentButton.innerHTML = agent.name.charAt(0); // Avatar = Primeira letra
            agentButton.onclick = () => toggleChat(`agent_${agent.id}`);
            
            const badge = document.createElement('div');
            badge.id = `agent-badge-${agent.id}`;
            badge.className = 'agent-badge';
            badge.style.display = 'none';
            
            agentButton.appendChild(badge);
            container.appendChild(agentButton);
            
            // Atualiza o badge
            updateAgentBadge(agent.id, agent.unread_count);
        });

        // 3. (Opcional) Iniciar um poller para notificações de agentes
        // (Similar ao pollForNotifications, mas chama /api/agents)
        
    } catch (error) {
        console.error("Erro ao carregar agentes:", error);
    }
}

function updateAgentBadge(agentId, count) {
    const badge = document.getElementById(`agent-badge-${agentId}`);
    if (badge) {
        if (count > 0) {
            badge.textContent = count;
            badge.style.display = 'flex';
        } else {
            badge.style.display = 'none';
        }
    }
}

function closeChatWindow() {
    const chatWindow = document.getElementById('chat-window');
    chatWindow.style.display = 'none';
    currentAgentContext = null;
    
    // Mostra o dock de agentes novamente
    document.getElementById('agent-dock-container').style.display = 'flex';
}

async function toggleChat(agentContext) {
    currentAgentContext = agentContext; // ex: 'rag' ou 'agent_1'
    const chatWindow = document.getElementById('chat-window');
    const messagesContainer = document.getElementById('chat-messages');
    const chatTitle = document.getElementById('chat-window-title');
    const chatInput = document.getElementById('chat-input');
    
    // Esconde o dock de botões
    document.getElementById('agent-dock-container').style.display = 'none';
    
    messagesContainer.innerHTML = '<div class="loading">Carregando...</div>';
    chatWindow.style.display = 'flex';
    
    if (agentContext === 'rag') {
        chatTitle.textContent = 'Assistente Cortex (RAG)';
        chatInput.placeholder = 'Pergunte sobre feedbacks...';
        messagesContainer.innerHTML = `
            <div class="chat-message bot-message">
                Olá! Como posso ajudar você a encontrar insights sobre seu time hoje? (RAG)
            </div>
        `;
    } else {
        const agentId = agentContext.split('_')[1];
        const agent = activeAgents.find(a => a.id == agentId);
        chatTitle.textContent = agent ? agent.name : 'Agente';
        chatInput.placeholder = `Converse com ${agent.name}...`;

        // Carregar histórico da conversa
        await loadAgentConversation(agentId);
        
        // Marcar como lido
        await fetch(`/api/agents/${agentId}/conversation/mark_read`, { method: 'POST', credentials: 'include' });
        updateAgentBadge(agentId, 0);
    }

    // Configurar o botão Enviar
    const sendButton = document.getElementById('chat-send-btn');
    sendButton.onclick = () => sendChatMessage(); // A função sendChatMessage usará 'currentAgentContext'
    
    const chatInputEl = document.getElementById('chat-input');
    chatInputEl.onkeydown = (event) => {
        if (event.key === 'Enter') {
            event.preventDefault();
            sendChatMessage();
        }
    };
}

async function loadAgentConversation(agentId) {
    const messagesContainer = document.getElementById('chat-messages');
    try {
        const response = await fetch(`/api/agents/${agentId}/conversation`, { credentials: 'include' });
        const data = await response.json();
        
        if (data.conversation && data.conversation.length > 0) {
            messagesContainer.innerHTML = data.conversation.map(msg => {
                if(msg.is_proactive) {
                    // Renderiza o insight proativo
                    return `<div class="chat-message bot-message proactive-insight">
                                <strong>💡 Insight do ${document.getElementById('chat-window-title').textContent}:</strong>
                                ${msg.text}
                                ${msg.action_data ? `<button class="action-button" onclick="handleAgentAction('${agentId}', ${msg.id}, '${JSON.stringify(msg.action_data)}')">Discutir Ação</button>` : ''}
                           </div>`;
                }
                return `<div class="chat-message ${msg.is_from_agent ? 'bot-message' : 'user-message'}">${msg.text}</div>`;
            }).join('');
        } else {
            messagesContainer.innerHTML = `<div class="chat-message bot-message">Olá! Eu sou ${document.getElementById('chat-window-title').textContent}. Como posso ajudar?</div>`;
        }
        messagesContainer.scrollTop = messagesContainer.scrollHeight;
    } catch (error) {
        messagesContainer.innerHTML = `<div class="chat-message bot-message">Erro ao carregar histórico.</div>`;
    }
}

async function sendChatMessage() {
    const input = document.getElementById('chat-input');
    const question = input.value.trim();
    if (!question) return;
    const messagesContainer = document.getElementById('chat-messages');
    messagesContainer.innerHTML += `<div class="chat-message user-message">${question}</div>`;
    input.value = '';
    messagesContainer.innerHTML += `<div class="chat-typing bot-message" id="typing-indicator"><span></span><span></span><span></span></div>`;
    messagesContainer.scrollTop = messagesContainer.scrollHeight;

    let url;
    let body;

    if (currentAgentContext === 'rag') {
        url = '/api/chat';
        body = JSON.stringify({ question });
    } else {
        const agentId = currentAgentContext.split('_')[1];
        url = `/api/agents/${agentId}/message`;
        body = JSON.stringify({ question });
    }
    
    try {
        const response = await fetch(url, {
            method: 'POST', headers: { 'Content-Type': 'application/json' }, credentials: 'include',
            body: body
        });
        const data = await response.json();

        document.getElementById('typing-indicator').remove();
        
        let responseText = "Desculpe, ocorreu um erro.";
        if (currentAgentContext === 'rag') {
            // A API RAG/n8n retorna um array
            responseText = data[0].output || data.error; 
        } else {
            // A API do Agente retorna um objeto
            responseText = data.response || data.error;
        }

        messagesContainer.innerHTML += `<div class="chat-message bot-message">${responseText}</div>`;
        messagesContainer.scrollTop = messagesContainer.scrollHeight;
    } catch (error) {
        document.getElementById('typing-indicator').remove();
        messagesContainer.innerHTML += `<div class="chat-message bot-message">Não consegui processar sua pergunta. Tente novamente.</div>`;
        messagesContainer.scrollTop = messagesContainer.scrollHeight;
    }
}

// (Função 'renderChatWidget' original foi removida/substituída por renderAgentDock)
// (Função 'toggleChat' original foi removida/substituída pela nova toggleChat e closeChatWindow)
// (Função 'sendChatMessage' original foi modificada para ser genérica)

// --- PONTO DE ENTRADA DA APLICAÇÃO ---
// (Sem mudanças aqui)
```